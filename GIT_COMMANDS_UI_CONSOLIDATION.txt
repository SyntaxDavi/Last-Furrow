# GIT COMMANDS - UI Consolidation

Execute these commands in PowerShell:

```bash
# Add all changes
git add -A

# Commit
git commit -m "refactor: consolidate pattern UI into PatternUIManager

- Created PatternUIManager as central UI coordinator
- Removed PatternScoreHUDView (redundant)
- Added decay/recreation event dispatching in AnalyzingPhaseController
- PatternFeedbackView now debug-only
- Build successful, 0 errors

PENDING MANUAL STEPS:
- Move files from Domain/Patterns/Visual/Controllers/ to UI/Patterns/Controllers/
- Update scene references (replace PatternTextPopupController with PatternUIManager)
- Delete old PatternTextPopupController.cs"

# Push
git push origin feature/pattern-visual-juice
```
